# Part C 签署范围说明（回应 Gate 1 负责人签核 §1/§2/§4）

> 起因：`TEAM_MEMBER_CACHE_GATE1_OWNER_SIGNOFF.zh-CN.md` §1 记录「C 签署 PASS ✅（收到提交声明）」，
> 并 §4 要求补「C 的原始签署证据与'PASS'适用范围说明」。
> 日期：2026-09-25。回应者：Agent C。依据：本仓 `TEAM_MEMBER_CACHE_NEXT_PART_C_RESULT.zh-CN.md`。

---

## 0. 直接回答

**C 没有签署过任何「Gate 1 PASS」，也没有见过待签的「V2 摘要包」。**

本仓中 C 侧的**唯一**签署性文件是 `TEAM_MEMBER_CACHE_NEXT_PART_C_RESULT.zh-CN.md`，
其总体状态是 **`CONDITIONAL`**，准入结论是 **`NO-GO / CONTINUE-SAMPLING`**（该文件 §0、§8.2）。
该文件**不存在**任何一处写着「Part C 通过」或「Gate 1 PASS」。

提交摘要 SHA-256 `797fd5bae6446b8d2b339e6d5e0d0d6815a593766f05282ee75e05e51ce34ed0`
所指向的正文**不在当前工作区**（全仓检索只命中该签核文件自身）。**C 未产生、未持有、无法复算该摘要。**

## 1. 为什么会出现这个说法（最可能的来源）

C 的报告 §0 是一张**逐项**判定表，其中若干行写着 `PASS`：

| 行 | 原文判定 | 它实际上在说什么 |
|---|---|---|
| C-1 | `PASS` | **交付项**：契约已冻结、逐臂配置断言已实跑 |
| C-3 | `PASS`（已冻结）/ 未达成 | **流程项**：门槛在看结果前冻结了；样本量**未达成** |
| C-7 | `PASS`（未触发） | **安全项**：本轮零超限事件 |

把「C 的某项交付检查为 PASS」读成「C 签署 Gate 1 PASS」，是把**逐项**判定升格成了**总体**签署。
C 的总体判定在同一张表的第 C-10 行，是 `NO-GO / CONTINUE-SAMPLING`。

## 2. 两个必须分开的语义（签核文件 §1 也提出了这一点）

| | **交付 / 流程 PASS** | **准入 / 灰度 GO** |
|---|---|---|
| 回答的问题 | 产物是否存在、可复算、口径是否按预注册执行 | **是否可以把行为变更放到真实用户面前** |
| C 能否签署 | **能**，且仅限 C 自己的产物 | **不能**——本轮结论是明确的不放行 |
| 依据 | §C.4 交付清单逐项落点（C 报告 §9） | §C.4 六项准入条件（C 报告 §8.1，三项不满足或不可测） |

**C 的签署范围止于左列，且逐项列出。** 右列 C 给出的是**否定**结论，不是许可。

## 3. C 可以为其真实性负责的产物

以下每一项都可由第三方在本仓独立复算：

| # | 产物 | 复算方式 | 实测 |
|---|---|---|---|
| 1 | 矩阵无混杂（`cache_aware_compaction` 全文固定） | `TestCacheAwareCompactionIsFixedAcrossTheMatrix` | PASS |
| 2 | A 因子只移动一个开关 | `TestAOnlyMovesTheLatchAndNothingElse` | PASS |
| 3 | 五个条件落到成员**自己的** agent | `TestEveryConditionReachesTheMembersOwnAgent` | 5/5 PASS |
| 4 | 实验契约（含内容哈希 `1de71fbc13b41ff4`） | `DraftContract` + `ContractDigest`，确定性渲染 | PASS |
| 5 | 五臂 live 执行与备份记录逐条对应 | C 报告 §10 的复算脚本 | 58/58 样本全等 |
| 6 | 原始证据完整性 | `sha256sum -c MANIFEST.sha256` | 44/44 通过 |
| 7 | 工程门禁 | build / vet / gofmt / repolint / 全套测试 | 全绿 |

**C 不对以下各项负责，也未签署它们**：A 的触发配方正确性、B 的分层与 128 结论、
V1→V2 摘要包的任何内容、`desktop/` 模块、以及任何跨账号/跨网关的外推。

## 4. 关于 §2「V1→V2 是否需要 C 重签」

**不适用。** 该问的前提是「C 对 V1 签过字」，而 C 并未签署过该摘要包的任何版本。
若该包确实需要 C 签署，请将其正文与 `§6` 签核块放入工作区；
C 可在核对以下四项后，**仅就该确切哈希**签署**交付层面**的意见：

1. 摘要实际 SHA-256 与提交值一致；
2. 该包引用的 C 侧数字与 C 报告逐项一致（C 报告 §10 的复算命令可验）；
3. 该包明确区分「交付 PASS」与「准入 GO」（见 §2）；
4. 该包不含任何要求 C 为 A/B 侧结论背书的表述。

**C 不会签署**：任何形式的灰度授权、生产默认值变更、或把 `pressure` profile 的结果
当作生产频率/收益的表述。

## 5. 结论的时效性

C 的判定**绑定它自己跑的那个快照**（`f53c4bf91` 加 C 的改动）。
若待签包基于另一个快照，则 §3 的复算需在该快照上重跑；
**快照不一致时，C 报告的数字不自动迁移到该包之上。**

---

## 6. 本文未做

- 未修改任何代码或数据；
- 未对 V2 摘要包作任何形式的签署或拒绝（该包不在工作区，无从核对）；
- 未改 C 报告的结论——`CONDITIONAL` / `NO-GO / CONTINUE-SAMPLING` 维持不变；
- 未提交、未推送。
