# Gate 1 负责人签核审查记录

> 审查日期：2026-09-25  
> 时区：Asia/Shanghai（UTC+08:00）  
> 待签对象：Gate 1 V2 摘要包  
> 提交的摘要 SHA-256：`797fd5bae6446b8d2b339e6d5e0d0d6815a593766f05282ee75e05e51ce34ed0`

## 1. 当前结论：暂缓签核

**目前不能签署 Gate 1 PASS。** 待签 V2 摘要包正文及其 §6 签核块未出现在当前工作区，故无法对照摘要复算 SHA-256，也无法确认 D-1 至 D-4 的定义、签核对象和时间窗语义。

此外，当前仓库中的 `TEAM_MEMBER_CACHE_NEXT_PART_C_RESULT.zh-CN.md` 明确记载 Part C 总体状态为 `CONDITIONAL`，准入结论为 `NO-GO / CONTINUE-SAMPLING`。这不必然否定“C 对某个交付检查签署 PASS”，但必须在待签包中明确区分“交付/流程 PASS”与“实验准入或灰度 GO”，并提供 C 的原始签署记录。不能仅凭状态表中的勾选消除该歧义。

| 项 | 提交状态 | 本次核验 |
|---|---|---|
| A 交付 preflight | ✅ | 收到提交声明；待签包/证据未核验 |
| B 完成 V2 | ✅ | 收到提交声明；V2 正文/摘要未核验 |
| C 签署 PASS | ✅ | 收到提交声明；本地 C 报告为 `CONDITIONAL`，需澄清 PASS 范围并附签署记录 |
| 负责人签核 | ⏸ | 暂缓，待下述材料齐备 |

## 2. V1→V2 是否需要 C 重签

**原则裁定：若确属方案 §4 定义的非实质性变更，不要求 C 重签；但现阶段无法确认本次变更满足该条件。**

提交说明称三处改动仅涉及引用与算术，且不影响门槛、样本量或结论方向。基于这一描述，倾向沿用 C 对 V1 的签名。最终确认前须提供：

- V1、V2 两份完整摘要及各自 SHA-256；
- 三处变更的逐项 diff，并标明对应方案 §4 的非实质性条款；
- C 的 V1 签署记录，以及该签署所覆盖的结论范围。

若 diff 显示任何准入判定、门槛、样本量/功效、停止规则、主指标方向或结论范围发生变化，则必须由 C 对 V2 重新签署。V1 签名与版本不得被覆盖。

## 3. D-4 时间窗

**暂不填写 D-4。** 目前未取得包 §6 的字段定义，无法判断 D-4 指签核有效期、采样时间窗还是其他时间范围。不得把签核日期擅自当作 D-4 的实验时间窗。

待签核包 §6 原文及其余 D-1/D-2/D-3 上下文提供后，再依据实验记录中的实际起止时间填写绝对日期、时区及边界约定；时间窗尚未发生或无法从证据确认时，不得虚构。

## 4. 完成签核所需材料

- 包含 §6 的完整 V2 摘要正文，且其实际 SHA-256 与提交值 `797fd5bae6446b8d2b339e6d5e0d0d6815a593766f05282ee75e05e51ce34ed0` 一致；
- V1→V2 三处变更清单/diff，以及方案 §4 对应规则；
- C 的原始签署证据与“PASS”适用范围说明；
- D-4 的准确含义及可验证的时间窗依据。

材料核验通过后，负责人签核仅针对该确切 V2 哈希，并须继续保留实验结论中的 `NO-GO / CONTINUE-SAMPLING`（如该 V2 内容仍如此）。负责人签核不授权扩大采样、生产默认值变更、灰度或发布。
